# 2026-07-04 工作日志

## 第 2 轮项目健康体检与改进计划（仅计划，未改代码）

- 任务：查看 docs 文件夹了解记录规则，对项目做 5 轮扫描的全面健康体检，产出改进计划文档（不修改代码）。
- 已读：docs/plans/2026-07-04-health-check-and-improvement-plan.md（第 1 轮计划）、RESULTS_LOG、DECISION_LOG、ARCHITECTURE、STRUCTURE_TREE、CODE_GUIDELINES、ACCEPTANCE_CHECKLIST、CHANGELOG、incremental.mbt、verify.mbt。
- 5 轮扫描结论：
  - 实测基线：79 提交 / 8368 行（实现 4024 + 测试 4344）/ 240 测试 / 12 包 / moon.mod 0.3.0 / wenlittle 比赛材料 0 处 / moon check 0 警告。
  - 三维度评分：源码 8.0 / 测试 7.5 / 文档治理 6.0 / 综合 7.2（第 1 轮为 7.5/6.5/4.5/6.2）。
  - 第 1 轮 18 项修复中 16 项治理生效、2 项复发（量化漂移、STRUCTURE_TREE 未真正重生成）。
  - 新发现 3 项：① 增量路径 incremental.mbt:49-52 计算 canonical_json 后 ignore 丢弃，跳过 E2004 manifest 摘要断言（主路径有），是功能性契约缺口；② moon.mod 版本 0.3.0 与 CHANGELOG 0.3.1 条目脱节；③ "单一事实源"机制是程序性非自动化，无 CI 门禁，3 个提交内即漂移复发。
- 产出：docs/plans/2026-07-04-health-check-round2-and-improvement-plan.md（4 阶段计划：自动防漂移门禁 → 增量契约对齐 → 版本与结构治理 → 竞争力推进）。
- PROJECT_INDEX 已加新计划引用。
- 关键经验：文档数字对齐必须靠 CI 自动门禁，靠人记得重跑必然复发。

## 全面升级与 UI 重设计方案（仅计划，未改代码）

- 任务：用户要求不要最小可改，要全面改进、创新点突出、竞争力强、UI 前端优化。产出方案文档。
- 现状勘察：demo/web/index.html 是 10KB 单页（选目录→验证→OK/FAILED 表格），GitHub 卡片风，无任何可视化。项目底层有 Merkle 树/版本链/Ed25519 签名/审计日志/内容寻址存储/包含证明——全部天生适合可视化但前端一个都没暴露，是最大创新缺口。
- 战略升级定位：从"验证库 + 简单 demo"升级为"可信证据工作台（Trust Workbench）"——让评委 30 秒内看见密码学如何保护文件完整性。同时满足 MoonBit 生态项目 / 区块链课程大项目 / OSC2026 竞赛三重身份。
- 6 个创新点：① Merkle 树交互式可视化 + 篡改实验室（改字节实时重算，最强展示钩子）；② 版本链时间轴 + DAG 预留；③ Ed25519 签名验证流程动画（纯 MoonBit 唯一实现）；④ 审计日志哈希链实时可视化；⑤ 从验证器到工作台（创建+证明+去重热力图）；⑥ 暗色模式 + PWA + 可访问性。
- 信息架构：多视图工作台（Verify/Create/Prove/Audit/Sign/Tamper Lab/Bench），原生 ES Modules 零框架，与 MoonBit 编译产物直接集成。
- 后端 API 扩展：需新增 compute_tree（返回完整 Merkle 树中间节点，唯一需改底层的项，纯增量）+ create/prove/audit/sign 的 JSON API 包装。
- 4 阶段执行：A 可视化基建+Tamper Lab（最高 ROI）→ B 创建/证明/审计/签名视图 → C 版本链+去重+性能图表 → D PWA+a11y+文档+演示视频。
- 产出：docs/plans/2026-07-04-comprehensive-upgrade-and-ui-redesign-plan.md。
- 关键判断：第 1/2 轮体检治"工程地基"，本方案建"展示上层"，两者缺一不可。地基不牢上层被否定，上层不亮地基拿不到展示分。

## MerkleTree 全树物化 + leaf_path（已实现）

- 任务：为 Tamper Lab 可视化提供遍历整棵 Merkle 树的能力，不改变 compute_root/verify_inclusion 的已有契约。
- 实现：
  - `compute_tree(leaves) -> MerkleTree?`：物化完整树（所有层级），None 对空输入，奇数节点提升规则与 compute_root 一致（CVE-2012-2459 防御）
  - `MerkleTree.root() / height() / leaf_count() / level(i)`：访问器，越界 level 返回 [] 无 panic
  - `PathStep` / `MerkleTree::leaf_path(index) -> Array[PathStep]?`：从叶子到根脊线，每层级一步，末尾是根；越界返回 None
  - 新增 11 个测试：空树 / 与 compute_root 等价性（8 种形状） / 单叶子 / 双叶子 / 三叶子奇节点提升 / 五叶子 4 层折叠 / 越界 level 安全 / leaf_path 结构校验 + 越界
  - 测试：240→251，wasm-gc 251 passed / js 251 passed，双后端全绿
- 关键设计决策：越界安全（返回 [] 而非 panic）、promoted node 不自我配对、public 仅露出原始不含全路径的 MerkleTree API、`pub(all)` 仅供工作台跨包使用

## Tamper Lab 篡改实验台页面（已实现 + 已启动）

- 任务：实现 demo/web/tamper-lab.html —— 交互式 Merkle 树可视化篡改实验台，使用 2024 年免费 Web 技术栈（HTML+CSS+JS ES Modules，零框架、零付费库、零 npm）。
- 状态：**已部署**，服务器在 `http://127.0.0.1:8000/demo/web/tamper-lab.html`，moon check 0 警告，wasm-gc 251 passed。
- 页面功能：
  - 文件上传区（支持 valid/tampered 证据包拖入）
  - 文件列表表格（文件名 / 算法 / 摘要 / 状态 ✓/✗）
  - Merkle 树可视化画布（纯 DOM 绘制，无 canvas/无三方库）：树节点分色（valid=绿、tampered=红、兄弟=灰、根=蓝）
  - 双击文件行或"定位在树中"按钮：高亮叶子→父→...→根脊线（leaf_path 动画路径）
  - 篡改工具面板（选择文件→切换字节按钮→查看根变化动画）
  - 错误码显示区域（发现列表 → E1004/E2001/E3001 等）
  - 版本链 + 签名 + 审计日志摘要卡片
- 实现策略：去重用 `createObjectURL` 手动 ref count，AI 排版用内联 CSS Grid + 语义颜色变量（light theme safe），无 npm、无第三方库、单 HTML 文件。
- 当前限制：moon 0.3.0 只产 debug 构建，release 路径用 cp 桥接；需 HTTP 服务器运行（file:// 不支持 ES module CORS）。
- 改 2 个文件：demo/web/tamper-lab.html（新写 ~15KB） + demo/README.md（说明桥接方式）。

## 进度盘点（2026-07-04 18:26）

对照两条主线计划的当前缺口：

### 主线 A（工程地基）剩余项
- ❌ 阶段 0：tools/check-metrics.mjs 自动防漂移门禁未做
- ❌ 阶段 1：incremental.mbt:49-52 仍 `ignore(canonical_json)`，跳过 E2004 manifest 摘要断言
- ❌ 阶段 2：git tag v0.4.0 未打；STRUCTURE_TREE.md:170 仍写"22 用例"（实际 41）；README.md:221 仍写"234 unit tests"（实际 251），README.md:236 仍写"7593 行"（实际 8966）；CHANGELOG 0.4.0 条目已加但文档数字未同步
- ❌ 阶段 3：演示视频脚本/录制未做；GitHub Pages 配置未做；CLI_VERSION CI 校验未做

### 主线 B（展示上层）剩余项
- ✅ 阶段 A.1-A.4：compute_tree/leaf_path/compute_merkle_tree API + Tamper Lab 页面已做
- ❌ 阶段 A.5：Verify 视图升级（拖拽+流水线动画+findings 联动）未做
- ❌ 阶段 B：Create/Prove/Audit/Sign 四视图全未做（src/api 中 create_evidence/generate_proof/audit_append/ed25519_keypair 包装 API 数=0）
- ❌ 阶段 C：版本链时间轴/去重热力图/性能图表全未做
- ❌ 阶段 D：PWA/暗色模式/a11y/视觉回归/README 截图全未做（tamper-lab.html 无 prefers-color-scheme）
- ❌ 视图间状态共享未做（每个页面独立）
