# cmd/main

Native CLI adapter.

Expected commands:

```text
moon-evidence verify <pack-path>
moon-evidence explain <pack-path>
```

The CLI should stay thin and delegate all verification logic to library packages.

