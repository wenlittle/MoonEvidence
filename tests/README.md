# Tests

Test layers:

- Unit tests for pure package behavior.
- Fixture tests for valid and tampered evidence packs.
- CLI black-box tests after `cmd/main` exists.
- Property tests for canonical JSON and Merkle proof behavior when MoonBit QuickCheck is added.

